define([
	"dojo/_base/declare",
	"../../md2_runtime/actions/_Action"
],
function(declare, _Action) {
	
	return declare([_Action], {
		
		_actionSignature: "__combined_startAction",
		
		execute: function() {
			
			var action0bx = this.$.actionFactory.getCustomAction("registerButtonEvents");
			action0bx.execute();
			
			var action0by = this.$.actionFactory.getCustomAction("registerAutogeneratedViewEvents");
			action0by.execute();
			
			var action0bz = this.$.actionFactory.getCustomAction("registerOnConditionEvent");
			action0bz.execute();
			
		}
		
	});
});
