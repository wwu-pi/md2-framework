define([
	"dojo/_base/declare",
	"../../md2_runtime/actions/_Action"
],
function(declare, _Action) {
	
	return declare([_Action], {
		
		_actionSignature: "__combined_startAction",
		
		execute: function() {
			
			var action02s = this.$.actionFactory.getCustomAction("registerButtonEvents");
			action02s.execute();
			
			var action02t = this.$.actionFactory.getCustomAction("registerAutogeneratedViewEvents");
			action02t.execute();
			
			var action02u = this.$.actionFactory.getCustomAction("registerOnConditionEvent");
			action02u.execute();
			
		}
		
	});
});
