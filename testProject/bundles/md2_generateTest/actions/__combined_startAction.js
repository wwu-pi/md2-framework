define([
	"dojo/_base/declare",
	"../../md2_runtime/actions/_Action"
],
function(declare, _Action) {
	
	return declare([_Action], {
		
		_actionSignature: "__combined_startAction",
		
		execute: function() {
			
			var action0q = this.$.actionFactory.getCustomAction("registerButtonEvents");
			action0q.execute();
			
			var action0r = this.$.actionFactory.getCustomAction("registerAutogeneratedViewEvents");
			action0r.execute();
			
			var action0s = this.$.actionFactory.getCustomAction("registerOnConditionEvent");
			action0s.execute();
			
			var action0t = this.$.actionFactory.getCustomAction("startWf");
			action0t.execute();
			
		}
		
	});
});
