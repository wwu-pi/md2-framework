define([
	"dojo/_base/declare",
	"../../md2_runtime/actions/_Action"
],
function(declare, _Action) {
	
	return declare([_Action], {
		
		_actionSignature: "__startupAction",
		
		execute: function() {
			
			var action0au = this.$.actionFactory.getCustomAction("__autoGenerationAction");
			action0au.execute();
			
			var action0av = this.$.actionFactory.getCustomAction("__conditionalEventRegister_evt");
			action0av.execute();
			
			var action0aw = this.$.actionFactory.getGotoViewAction("autogeneratedView");
			action0aw.execute();
			
			var action0ax = this.$.actionFactory.getCustomAction("__combined_startAction");
			action0ax.execute();
			
		}
		
	});
});
