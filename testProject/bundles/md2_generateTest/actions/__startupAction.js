define([
	"dojo/_base/declare",
	"../../md2_runtime/actions/_Action"
],
function(declare, _Action) {
	
	return declare([_Action], {
		
		_actionSignature: "__startupAction",
		
		execute: function() {
			
			var action02v = this.$.actionFactory.getCustomAction("__autoGenerationAction");
			action02v.execute();
			
			var action02w = this.$.actionFactory.getCustomAction("__conditionalEventRegister_evt");
			action02w.execute();
			
			var action02x = this.$.actionFactory.getGotoViewAction("autogeneratedView");
			action02x.execute();
			
			var action02y = this.$.actionFactory.getCustomAction("__combined_startAction");
			action02y.execute();
			
		}
		
	});
});
