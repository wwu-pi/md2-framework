define([
	"dojo/_base/declare",
	"../../md2_runtime/actions/_Action"
],
function(declare, _Action) {
	
	return declare([_Action], {
		
		_actionSignature: "__startupAction",
		
		execute: function() {
			
			var action03f = this.$.actionFactory.getCustomAction("__autoGenerationAction");
			action03f.execute();
			
			var action03g = this.$.actionFactory.getCustomAction("__conditionalEventRegister_evt");
			action03g.execute();
			
			var action03h = this.$.actionFactory.getCustomAction("__registerWorkflowActionEventTrigger");
			action03h.execute();
			
			var action03i = this.$.actionFactory.getGotoViewAction("autogeneratedView");
			action03i.execute();
			
			var action03j = this.$.actionFactory.getCustomAction("__combined_startAction");
			action03j.execute();
			
		}
		
	});
});
